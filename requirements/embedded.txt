# Embedded requirements
#
# Typical command to install:
# python -m pip install --no-deps -U -r requirements/embedded.txt -t qgis_resource_sharing/ext_libs
# -----------------------

dulwich>=0.20,<0.21
giturlparse>=0.9,<0.11
pathvalidate>=3.1,<3.3
